export enum AuthProvider {
  Google = 'Google',
  Facebook = 'Facebook',
  // Github = 'Github',
  // Apple = 'Apple',
}

export enum AuthModule {
  Admin = 1,
  Clinic = 2,
  Patient = 3,
  Guest = 4,
}
